package com.djpedersen.mgyoutube.behavior_tests;

public class ChildrensAppBasicSteps extends BaseSteps {

	// @Given("a child user (.*) with password (.*)$")
	// public void givenAChildUserAndPassword(final String username, final String
	// password) throws IOException {
	// final User user = User.createChildUser(username, password);
	// final User childUser = ensureUserExists(user);
	// childUser.password = password;
	// addAUserToScenario(user);
	// }

}
